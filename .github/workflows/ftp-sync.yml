on: workflow_dispatch
name: 🚀 FTP-SYNC
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.1.0
      with:
        server: ftp.p4783.phpnet.org
        username: p4783
        password: ${{ secrets.ftp_password }}
        protocol: ftp
        server-dir: www/nuitducodedotnet/
        #exclude:
        #  - .git*
        #  - README.txt
